import React, { Fragment, } from 'react';

const Dashboard = () => {

    return (
        <Fragment>
            <h1>This is a dashboard :(</h1>
        </Fragment>
    );
};

export default Dashboard;